// ! Selecting elements using get element by id

const conferenceTitleElement = document.getElementById("conferenceTitle");
const conferenceDateElement = document.getElementById("conferenceDate");
const locationTitleElement = document.getElementById("locationTitle");
const conferenceLocationElement = document.getElementById("conferenceLocation");
const speakersTitleElement = document.getElementById("speakersTitle");
const speakerListElement = document.getElementById("speakerList");

console.log(
  conferenceTitleElement,
  conferenceDateElement,
  locationTitleElement,
  conferenceLocationElement,
  speakersTitleElement,
  speakerListElement
);